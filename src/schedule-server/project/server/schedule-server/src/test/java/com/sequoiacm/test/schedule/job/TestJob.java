package com.sequoiacm.test.schedule.job;

import org.testng.annotations.Test;

public class TestJob {
    @Test
    public void Test() throws Exception {

        // ScheduleMgr mgr = new QuartzScheduleMgr();
        // String id1 = "1";
        // mgr.createJob(id1, ScheduleDefine.ScheduleType.CLEAN_FILE, "w1", new
        // BasicBSONObject("k1",
        // "v1"), "* * * * * ?");
        //
        // String id2 = "2";
        // mgr.createJob(id2, ScheduleDefine.ScheduleType.COPY_FILE, "w2", new
        // BasicBSONObject("k2",
        // "v2"), "* * * * * ?");
        //
        // ScheduleJobInfo info1 = mgr.getJobInfo(id1);
        // ScheduleJobInfo info2 = mgr.getJobInfo(id2);
        //
        // mgr.start();
        //
        // Thread.sleep(10000);
        //
        // mgr.deleteJob(id2);
        //
        // Thread.sleep(10000);
        //
        // mgr.clear();
    }
}
